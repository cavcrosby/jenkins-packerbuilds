#!/bin/bash

PACKER_HTTP_PATH="${WORKSPACE}/${HOSTNAME}/http"
PACKER_SHELL_PROVISIONER_PATH="${WORKSPACE}/${HOSTNAME}/scripts"
PACKER_BUILD_ROOT_PATH="${WORKSPACE}/${HOSTNAME}"
mkdir -p "$PACKER_HTTP_PATH"
mkdir -p "$PACKER_SHELL_PROVISIONER_PATH"
