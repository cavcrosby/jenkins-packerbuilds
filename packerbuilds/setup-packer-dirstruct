#!/bin/bash

PACKER_HTTP_PATH="${WORKSPACE}/${PACKERBUILD_NAME}/http"
PACKER_SHELL_PROVISIONER_PATH="${WORKSPACE}/${PACKERBUILD_NAME}/scripts"
PACKER_BUILD_ROOT_PATH="${WORKSPACE}/${PACKERBUILD_NAME}"
mkdir -p "$PACKER_HTTP_PATH"
mkdir -p "$PACKER_SHELL_PROVISIONER_PATH"
